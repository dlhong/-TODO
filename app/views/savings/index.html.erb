<div class="container-index">
  <div class="index-display">
  <ul>
    <% @savings.each do |saving| %>
      <li><%= saving.user_id%>,
      <%= saving.activity_id%>,
      <%= saving.attendance%>
      <%= current_user.id%></li>
    <% end %>
  </ul>
  </div>
</div>
